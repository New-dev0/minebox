import { AnimatedBackground } from '../backgrounds'

export default function AuthBackground() {
  return (
    <div className="fixed inset-0 z-0">
      <AnimatedBackground sceneType="saturn" />
    </div>
  )
} 